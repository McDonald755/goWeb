package db

func InsertGame() *[]Game {
	var result []Game
	//
	//res := config.DB.Table("game").Where("type = ? and status = ? and deleted = 0", t, status).Find(&result)
	//if res.Error != nil {
	//	log.Error(res.Error)
	//}
	return &result
}

func DeleteGame() *[]Game {
	var result []Game
	//
	//res := config.DB.Table("game").Where("type = ? and status = ? and deleted = 0", t, status).Find(&result)
	//if res.Error != nil {
	//	log.Error(res.Error)
	//}
	return &result
}

func UpdateGame() *[]Game {
	var result []Game
	//
	//res := config.DB.Table("game").Where("type = ? and status = ? and deleted = 0", t, status).Find(&result)
	//if res.Error != nil {
	//	log.Error(res.Error)
	//}
	return &result
}

func GetGames() *[]Game {
	var result []Game
	//
	//res := config.DB.Table("game").Where("type = ? and status = ? and deleted = 0", t, status).Find(&result)
	//if res.Error != nil {
	//	log.Error(res.Error)
	//}
	return &result
}

func GetGameDetail() *[]Game {
	var result []Game
	//
	//res := config.DB.Table("game").Where("type = ? and status = ? and deleted = 0", t, status).Find(&result)
	//if res.Error != nil {
	//	log.Error(res.Error)
	//}
	return &result
}
